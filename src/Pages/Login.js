import React from "react";
import AuthForm from "../components/Auth/AuthForm";

const Login = () => {
  return (
    <React.Fragment>
      <AuthForm />
    </React.Fragment>
  );
};

export default Login;
